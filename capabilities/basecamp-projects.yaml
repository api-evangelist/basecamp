naftiko: 1.0.0-alpha2
info:
  label: Basecamp API — Projects
  description: 'Basecamp API — Projects. 5 operations. Lead operation: List projects. Self-contained Naftiko capability covering
    one Basecamp business surface.'
  tags:
  - Basecamp
  - Projects
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BASECAMP_API_KEY: BASECAMP_API_KEY
capability:
  consumes:
  - type: http
    namespace: basecamp-projects
    baseUri: https://3.basecampapi.com/{accountId}
    description: Basecamp API — Projects business capability. Self-contained, no shared references.
    resources:
    - name: projects.json
      path: /projects.json
      operations:
      - name: listprojects
        method: GET
        description: List projects
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: status
          in: query
          type: string
          description: Filter projects by status. Omit for active projects.
      - name: createproject
        method: POST
        description: Create a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: projects-projectId}.json
      path: /projects/{projectId}.json
      operations:
      - name: getproject
        method: GET
        description: Get a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updateproject
        method: PUT
        description: Update a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteproject
        method: DELETE
        description: Delete a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.BASECAMP_API_KEY}}'
  exposes:
  - type: rest
    namespace: basecamp-projects-rest
    port: 8080
    description: REST adapter for Basecamp API — Projects. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/projects-json
      name: projects-json
      description: REST surface for projects.json.
      operations:
      - method: GET
        name: listprojects
        description: List projects
        call: basecamp-projects.listprojects
        with:
          status: rest.status
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createproject
        description: Create a project
        call: basecamp-projects.createproject
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/projects/projectid-json
      name: projects-projectid-json
      description: REST surface for projects-projectId}.json.
      operations:
      - method: GET
        name: getproject
        description: Get a project
        call: basecamp-projects.getproject
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updateproject
        description: Update a project
        call: basecamp-projects.updateproject
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteproject
        description: Delete a project
        call: basecamp-projects.deleteproject
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: basecamp-projects-mcp
    port: 9090
    transport: http
    description: MCP adapter for Basecamp API — Projects. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: list-projects
      description: List projects
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: basecamp-projects.listprojects
      with:
        status: tools.status
      outputParameters:
      - type: object
        mapping: $.
    - name: create-project
      description: Create a project
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: basecamp-projects.createproject
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-project
      description: Get a project
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: basecamp-projects.getproject
      outputParameters:
      - type: object
        mapping: $.
    - name: update-project
      description: Update a project
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: basecamp-projects.updateproject
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-project
      description: Delete a project
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: basecamp-projects.deleteproject
      outputParameters:
      - type: object
        mapping: $.
